version: '3.8'

services:
  # SCIM Server application
  scimserver:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: scimserver
    ports:
      - "5000:80"
      - "5001:443"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:80;https://+:443
      - ConnectionStrings__DefaultConnection=Server=sqlserver;Database=SCIMServer;User Id=sa;Password=YourStrong!Passw0rd;TrustServerCertificate=True
      - Jwt__SecretKey=ThisIsADockerSecretKeyChangeInProduction!
    depends_on:
      sqlserver:
        condition: service_healthy
    networks:
      - scimserver-network
    volumes:
      - scimserver-logs:/app/logs
      - scimserver-config:/app/config

  # SQL Server database
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: scimserver-db
    ports:
      - "1433:1433"
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=YourStrong!Passw0rd
      - MSSQL_PID=Express
    volumes:
      - sqlserver-data:/var/opt/mssql
    networks:
      - scimserver-network
    healthcheck:
      test: ["CMD", "/opt/mssql-tools/bin/sqlcmd", "-S", "localhost", "-U", "sa", "-P", "YourStrong!Passw0rd", "-Q", "SELECT 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  # Optional: Adminer for database management
  adminer:
    image: adminer
    container_name: scimserver-adminer
    ports:
      - "8080:8080"
    environment:
      - ADMINER_DEFAULT_SERVER=sqlserver
    networks:
      - scimserver-network
    depends_on:
      - sqlserver

networks:
  scimserver-network:
    driver: bridge

volumes:
  sqlserver-data:
  scimserver-logs:
  scimserver-config: